{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "cppdbg",
      "request": "launch",
      "name": "Debug MLP Executable",
      "program": "${workspaceFolder}/build-debug/${input:mlpExecutable}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false
    }
  ],
  "inputs": [
    {
      "id": "mlpExecutable",
      "type": "pickString",
      "description": "Select the executable to debug",
      "options": [
        "mlp"
      ]
    }
  ]
}